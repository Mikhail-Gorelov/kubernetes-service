apiVersion: v1
kind: Service
metadata:
  name: {{ include "pg_backup.fullname" . }}
  labels:
    {{- include "pg_backup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pg_backup.selectorLabels" . | nindent 4 }}
