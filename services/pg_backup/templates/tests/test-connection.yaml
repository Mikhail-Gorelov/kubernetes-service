apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pg_backup.fullname" . }}-test-connection"
  labels:
    {{- include "pg_backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pg_backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
