apiVersion: v1
kind: Service
metadata:
  name: {{ include "posgresql.fullname" . }}
  labels:
    {{- include "posgresql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "posgresql.selectorLabels" . | nindent 4 }}
